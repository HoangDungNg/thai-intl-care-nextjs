export const SLIDER_CONFIG = {
  cardGapPx: 30, // visual gap between adjacent cards (px) — informs spreadDeg
  radiusPx: 540, // radius of the imaginary circle cards orbit around (px)
  spreadDeg: 16, // degrees of arc between adjacent cards
  animDuration: 0.55, // GSAP tween duration (seconds)
  cardW: 155, // card width (px); height = cardW × 16/9
};
